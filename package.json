{
  "name": "enumer",
  "version": "0.0.1",
  "description": "拓展ts枚举，结合vscode的enum-more插件使用",
  "main": "index.js",
  "devDependencies": {
    "esbuild": "^0.18.6",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts-types": "tsc  --emitDeclarationOnly --outDir dist",
    "build": "rimraf dist && node script/build.js && npm run ts-types"
  },
  "keywords": [
    "enum",
    "ts",
    "vscode"
  ],
  "author": "gao",
  "license": "ISC",
  "dependencies": {
    "-": "^0.0.1",
    "D": "^1.0.0",
    "prettier": "^2.8.8"
  }
}
